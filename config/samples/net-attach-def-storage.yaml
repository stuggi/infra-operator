apiVersion: k8s.cni.cncf.io/v1
kind: NetworkAttachmentDefinition
metadata:
  name: storage-dhcp
  namespace: openstack
spec:
  config: |-
    {
      "name": "storage-dhcp",
      "cniVersion": "0.3.1",
      "type": "macvlan",
      "master": "enp6s0.21",
      "ipam": {
        "type": "dhcp",
        "provide": [
          {
            "option": "host-name",
            "fromArg": "K8S_POD_NAME"
          }
        ]
      }
    }
